# 捨てた案は履歴にしかない

コードは選ばれた案だけを保存する。捨てた案は履歴が持つ。決断は選んだ案と捨てた案の対で
成立するため、捨てた案を要する決断はコード単体からは復元できない。差戻し、収束した
変更列、書き換えの前後が捨てた案にあたる。

これは決断がコードに無いという意味ではない。コードが指している決断の写像は書ける。
定義のように、捨てた案を持たずに成立する決断がある。

codifier がコードと履歴の両方を入力に取るのはこのため。
