# 捨てた案は履歴にしかない

コードは選ばれた案だけを保存する。捨てた案は履歴が持つ。decision は選んだ案と捨てた案の
対で成立するため、コード単体からは復元できない。差戻し、収束した変更列、書き換えの前後が
捨てた案にあたる。

これは条項がコードに無いという意味ではない。コードが指している条項の写像は書ける。
definition がそれで、捨てた案を持たずに成立する。条項が decision と definition に
分かれるのはこの線による。

codifier がコードと履歴の両方を入力に取るのはこのため。
