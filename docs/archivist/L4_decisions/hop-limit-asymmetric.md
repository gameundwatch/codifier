# 近傍図は出る辺を三ホップ、入る辺を一ホップとする

自分が根拠にした側は三ホップ辿り、自分を根拠にしている側は直接の一ホップだけを描く。

入次数は粒度そのものなので、入る辺を深く辿ると、粒度が上がるほど図が広がる。
全体図を捨てた理由が principle のファイルの中で再発する。

出る辺の数は粒度に依らないため、深く辿っても広がらない。
粒度ごとにホップ数を変えないのは、規則を一つに保つため。
