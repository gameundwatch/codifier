# 索引は CSV で持つ

`docs/codifier/` に索引を一枚置き、全条項を CSV で持つ。

~~列は path・statement・granularity・grounds の四つとする。~~

条項を一件足すたびに全ファイルを読むと、件数が増えたところで読み切れなくなる。
索引が一枚あれば、重複の検出も粒度の算出も循環の検査も、一度の読みで済む。

入次数は grounds 列を数えれば出るため、列に持たない。
