# 決断は発効時刻を欄として持つ

決断は statement・timecode・grounds を粒度によらず持ち、粒度に応じて rationale を加える。
timecode は徴候が成立したコミットの時刻を指す。

索引は実行のたびに作り直される。timecode が決断ファイルに無ければ、作り直した索引が
値を失い、先発の判定ができなくなる。

決断ファイルが元本であり、索引はそこから導かれる。索引を消しても決断ファイルさえ
あれば同じ索引が戻る。
