# 設定は `docs/codifier/config.json` に置く

エスカレーションの閾値など利用者が決める値を、`docs/codifier/config.json` に置く。

出力先の外に置くと、読む場所と書く場所が分かれる。固定した一箇所にまとまっていれば、
どこを見ればよいかが一つで済む。

条項を一列に並べる規則は条項ファイルに対するもので、設定ファイルはその対象ではない。
