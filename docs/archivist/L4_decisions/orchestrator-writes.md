# 書き込みはオーケストレーターが持つ

`/codifier` が四本を呼び、`docs/codifier/` への書き込みを自ら行う。四本はファイルに
触れず、書く内容を返すだけとする。

書き込みだけを単体で起動する場面が無い。内容を決める側が必ず先に居るため、受け身の
一本を単体のコマンドとして名指す理由が無い。

出力先の固定、一列の配置、索引の作り直し、近傍図の生成は、すべてこの一本が守る。
