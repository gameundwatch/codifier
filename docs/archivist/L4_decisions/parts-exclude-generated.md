# Parts に自動生成物を挙げない

design の Parts には実装が持つファイルだけを挙げ、実行時に生まれるファイルを挙げない。

印が外れる条件は Parts のファイルが在ることなので、生成物を挙げると、印が実装の
有無ではなく実行の有無を測ることになる。既定値があって無くてもよいファイルを挙げれば、
印は永久に外れない。

生成物に触ることは Rules と Relation に書く。記述はするが、実在を印の条件にしない。
